# ─── Docker Compose environment ──────────────────────────────────────────────
# Copy this to .env at the repo root to override Docker Compose defaults.

# PostgreSQL
POSTGRES_USER=fiction_aget
POSTGRES_PASSWORD=fiction_aget
POSTGRES_DB=fiction_aget

# API
LOG_LEVEL=INFO
DEBUG=false
